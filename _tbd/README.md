# _tbd — holding area (evaluate, then keep-or-delete)

Parked content from retiring workspaces, not wired into pdomain-ops. Evaluate and either
route into the right place or delete.

## ocr-container-docs/
The full `docs/` tree (221 files, ~5 MB) from the old OCR mega-container's meta repo
`ConcaveTrillion/ocr-container-meta` (GitHub remote no longer exists). Salvaged 2026-07-09.
Compared basename-by-basename against every greenfield's docs/: effectively ALL unique
(only 5 generic-name coincidences like README.md/.gitkeep). Content = pdomain/OCR project
history: archive/ (92 plans+decisions), research/ (54), plans/process/specs/architecture
for pd-book-tools, label-taxonomy, prep-for-pgdp, the design-system, etc.

TBD: cherry-pick anything still useful into the right pdomain repo; delete the rest.
